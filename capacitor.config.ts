import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'skeleton-app-ng-modules',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
